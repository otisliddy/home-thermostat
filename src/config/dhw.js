// The cylinder cannot be driven above 50; the immersion's own thermostat cuts out around 51.
export const DHW_MIN_TARGET_C = 30;
export const DHW_MAX_TARGET_C = 50;
export const DHW_TARGET_STEP_C = 0.5;
